# --- Clés API du moteur Bob.io ---
# Copie ce fichier en ".env.local" puis colle tes clés ci-dessous.

# Transcription (Groq Whisper) — gratuit : https://console.groq.com/keys
GROQ_API_KEY=

# Détection des moments forts — par défaut Groq (GRATUIT, même clé ci-dessus).
# Claude est OPTIONNEL : ne le remplis que si tu veux la détection premium.
# Il faut alors aussi mettre BOB_MOMENTS_PROVIDER=claude ci-dessous.
ANTHROPIC_API_KEY=

# Espace administrateur (/admin) — change ce mot de passe.
ADMIN_PASSWORD=bob-admin

# --- Comptes utilisateurs (optionnel — Supabase) ---
# Sans ces variables, l'app marche sans comptes. Avec, l'atelier demande une
# connexion et chaque utilisateur retrouve "Mes clips".
# NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=     # secret serveur — ne jamais exposer au navigateur

# --- Stockage permanent des clips (optionnel, compatible S3) ---
# Sans ces variables, les clips sont servis depuis le disque local (temporaires
# sur un hébergeur gratuit). Avec, ils sont conservés durablement dans le cloud.
# Compatible Cloudflare R2, Supabase Storage, Backblaze B2, etc.
# BOB_S3_ENDPOINT=https://<compte>.r2.cloudflarestorage.com
# BOB_S3_REGION=auto
# BOB_S3_BUCKET=bob-clips
# BOB_S3_ACCESS_KEY_ID=
# BOB_S3_SECRET_ACCESS_KEY=
# BOB_S3_PUBLIC_URL=https://<url-publique-du-bucket>

# --- Cookies YouTube (optionnel) ---
# Permet le téléchargement par LIEN YouTube depuis un serveur cloud (sinon bloqué).
# Colle le contenu d'un fichier cookies.txt (format Netscape) exporté d'un
# navigateur connecté à YouTube (de préférence un compte secondaire). Peut être
# en clair (multi-lignes) ou encodé en base64. Voir COOKIES_SETUP.md.
# BOB_YT_COOKIES=

# Optionnel : changer de fournisseur / modèles
# BOB_MOMENTS_PROVIDER=claude          # défaut: groq (gratuit)
# BOB_GROQ_MODEL=whisper-large-v3-turbo
# BOB_GROQ_LLM_MODEL=llama-3.3-70b-versatile
# BOB_CLAUDE_MODEL=claude-sonnet-4-6
